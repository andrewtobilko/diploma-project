spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/diploma-project
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: create
server:
  port: 5000

